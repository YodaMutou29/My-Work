#include <iostream>
using namespace std;

int main() {
    int anthonyPoints, coraPoints;
    cin >> anthonyPoints >> coraPoints;
    for (int i = 0; i < (anthonyPoints + coraPoints - 1); ++i) {
        double roundProbability;
        cin >> roundProbability;
        
    }
    return 0;
}